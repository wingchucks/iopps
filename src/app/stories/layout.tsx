import type { Metadata } from "next";
export const metadata: Metadata = { title: "Success Stories", description: "Read inspiring Indigenous success stories and partner spotlights. Discover how IOPPS members are making an impact across Canada." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
