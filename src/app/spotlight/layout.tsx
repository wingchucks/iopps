import type { Metadata } from "next";
export const metadata: Metadata = { title: "Spotlight — Videos & Livestreams" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
