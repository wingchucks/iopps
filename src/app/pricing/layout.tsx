import type { Metadata } from "next";
export const metadata: Metadata = { title: "Pricing & Plans", description: "Explore IOPPS pricing plans for employers and organizations. Post jobs, promote events, and reach Indigenous talent across Canada." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
