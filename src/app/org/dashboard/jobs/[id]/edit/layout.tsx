import type { Metadata } from "next";
export const metadata: Metadata = { title: "Edit Job Posting", description: "Edit your organization's job posting details, requirements, and application settings on IOPPS." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
