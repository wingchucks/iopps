import type { Metadata } from "next";
export const metadata: Metadata = { title: "Organization Dashboard", description: "Manage your organization's job postings, events, and team on the IOPPS employer dashboard." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
