import type { Metadata } from "next";
export const metadata: Metadata = { title: "Organization", description: "Manage your organization on IOPPS. Post jobs, share events, and connect with Indigenous professionals across Canada." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
