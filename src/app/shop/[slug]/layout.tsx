import type { Metadata } from "next";
export const metadata: Metadata = { title: "Shop Listing", description: "View product details from this Indigenous-owned business. Support Indigenous entrepreneurs on the IOPPS marketplace." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
