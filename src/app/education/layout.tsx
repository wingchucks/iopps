import type { Metadata } from "next";
export const metadata: Metadata = { title: "Education", description: "Explore Indigenous education resources, courses, and learning opportunities available through IOPPS partner institutions." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
