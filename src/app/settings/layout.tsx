import type { Metadata } from "next";
export const metadata: Metadata = { title: "Settings", description: "Manage your IOPPS account settings, notification preferences, privacy options, and profile configuration." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
