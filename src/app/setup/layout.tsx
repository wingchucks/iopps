import type { Metadata } from "next";
export const metadata: Metadata = { title: "Profile Setup", description: "Complete your IOPPS profile setup. Add your skills, experience, and community details to get started." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
