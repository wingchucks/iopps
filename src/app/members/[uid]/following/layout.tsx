import type { Metadata } from "next";
export const metadata: Metadata = { title: "Following", description: "See who this IOPPS member follows. Browse Indigenous professionals and organizations in their network." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
