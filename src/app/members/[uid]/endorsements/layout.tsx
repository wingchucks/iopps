import type { Metadata } from "next";
export const metadata: Metadata = { title: "Member Endorsements", description: "View endorsements and recommendations for this IOPPS community member from colleagues and organizations." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
