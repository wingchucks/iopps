import type { Metadata } from "next";
export const metadata: Metadata = { title: "Member Profile", description: "View this IOPPS member's professional profile, skills, endorsements, and community connections." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
