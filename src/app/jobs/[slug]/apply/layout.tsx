import type { Metadata } from "next";
export const metadata: Metadata = { title: "Apply for Job", description: "Submit your application for this Indigenous career opportunity. Upload your resume and cover letter through IOPPS." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
