import type { Metadata } from "next";
export const metadata: Metadata = { title: "Job Opportunity", description: "View full job details, requirements, and application instructions for this Indigenous career opportunity on IOPPS." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
