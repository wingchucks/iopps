import type { Metadata } from "next";
export const metadata: Metadata = { title: "Jobs — Indigenous Career Opportunities", description: "Browse Indigenous job postings across Canada. Find careers with Indigenous organizations, First Nations, and employers committed to Indigenous hiring." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
