import type { Metadata } from "next";
export const metadata: Metadata = { title: "Messages", description: "Send and receive messages with IOPPS community members, employers, and organizations. Stay connected with your network." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
