import type { Metadata } from "next";
export const metadata: Metadata = { title: "Scholarships — Funding & Awards", description: "Find scholarships, bursaries, and grants for Indigenous students and entrepreneurs in Canada. Review current eligibility and official application details.", alternates: { canonical: "/scholarships" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
