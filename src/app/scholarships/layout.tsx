import type { Metadata } from "next";
export const metadata: Metadata = { title: "Scholarships — Funding & Awards", description: "Find scholarships, bursaries, and grants for Indigenous students and entrepreneurs in Canada. Apply for funding opportunities today." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
