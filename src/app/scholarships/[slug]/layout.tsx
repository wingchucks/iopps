import type { Metadata } from "next";
export const metadata: Metadata = { title: "Scholarship Opportunity", description: "View scholarship details, eligibility requirements, deadlines, and application instructions for Indigenous students." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
