import type { Metadata } from "next";
export const metadata: Metadata = { title: "Event", description: "View event details, schedule, and RSVP for this Indigenous community gathering, pow wow, or career fair on IOPPS." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
