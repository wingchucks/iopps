import type { Metadata } from "next";
export const metadata: Metadata = { title: "Organizations", description: "Browse Indigenous organizations, First Nations, tribal councils, and employers listed on IOPPS across Canada.", alternates: { canonical: "/organizations" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
