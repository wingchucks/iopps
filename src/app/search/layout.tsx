import type { Metadata } from "next";
export const metadata: Metadata = { title: "Search", description: "Search IOPPS for Indigenous jobs, events, scholarships, training programs, organizations, and community members across Canada." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
