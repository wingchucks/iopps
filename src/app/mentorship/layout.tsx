import type { Metadata } from "next";
export const metadata: Metadata = { title: "Mentorship", description: "Find Indigenous mentors or become one. Connect with experienced professionals for career guidance and personal growth on IOPPS." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
