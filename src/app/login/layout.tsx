import type { Metadata } from "next";
export const metadata: Metadata = { title: "Sign In", description: "Sign in to your IOPPS account to access Indigenous job postings, events, scholarships, and connect with your community." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
