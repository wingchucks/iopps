import type { Metadata } from "next";
export const metadata: Metadata = { title: "My Profile", description: "View and edit your IOPPS profile. Update your skills, experience, education, and connect with Indigenous professionals." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
