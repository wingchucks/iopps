import type { Metadata } from "next";
export const metadata: Metadata = { title: "My Applications", description: "Track your job applications submitted through IOPPS. View application status and history for Indigenous career opportunities." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
