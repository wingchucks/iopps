import type { Metadata } from "next";
export const metadata: Metadata = { title: "Notifications", description: "View your IOPPS notifications for new job matches, event updates, endorsements, and community activity." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
