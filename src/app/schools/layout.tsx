import type { Metadata } from "next";
export const metadata: Metadata = { title: "Schools & Education Partners" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
