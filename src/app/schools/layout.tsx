import type { Metadata } from "next";
export const metadata: Metadata = { title: "Schools & Education Partners", description: "Discover Indigenous education partners, colleges, universities, and training institutions offering programs for Indigenous students in Canada." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
