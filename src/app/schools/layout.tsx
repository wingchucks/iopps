import type { Metadata } from "next";
export const metadata: Metadata = { title: "Schools & Education Providers", description: "Discover colleges, universities, and training institutions offering programs for Indigenous students in Canada.", alternates: { canonical: "/schools" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
