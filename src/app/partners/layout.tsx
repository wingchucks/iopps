import type { Metadata } from "next";
export const metadata: Metadata = { title: "Partners", description: "Meet the organizations partnering with IOPPS to support Indigenous employment, education, and community development across Canada." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
