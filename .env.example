# ==============================================================================
# IOPPS Environment Variables Template
# ==============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!
# ==============================================================================

# ------------------------------------------------------------------------------
# Firebase Client SDK (PUBLIC - Safe to expose in browser)
# ------------------------------------------------------------------------------
# These variables are prefixed with NEXT_PUBLIC_ and will be embedded in the
# client-side JavaScript bundle. They are NOT secret.

NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdef123456

# ------------------------------------------------------------------------------
# Firebase Admin SDK (PRIVATE - Server-side only)
# ------------------------------------------------------------------------------
# These are used by the Firebase Admin SDK on the server.
# Keep these SECRET! They provide full access to your Firebase project.

FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
# Important: The private key must have \n characters, not actual newlines
# In your Vercel/hosting dashboard, paste the FULL key including quotes
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYourKeyHere\n-----END PRIVATE KEY-----\n"

# ------------------------------------------------------------------------------
# Stripe (PRIVATE - Server-side only, except publishable key)
# ------------------------------------------------------------------------------
# Secret key: Full access to your Stripe account - NEVER expose to client!
STRIPE_SECRET_KEY=sk_live_... (or sk_test_... for development)

# Publishable key: Safe to expose in browser - use for Stripe Checkout
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_... (or pk_test_... for development)

# Webhook secret: Used to verify webhook events from Stripe
# Get this from Stripe Dashboard > Developers > Webhooks > Select endpoint > Signing secret
STRIPE_WEBHOOK_SECRET=whsec_...

# ------------------------------------------------------------------------------
# Site Configuration
# ------------------------------------------------------------------------------
# Production: https://iopps.ca
# Development: http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# Security & Access Control (PRIVATE - Server-side only)
# ------------------------------------------------------------------------------
# Super admin emails - comma-separated list of emails with full admin access
# Note: This was moved from NEXT_PUBLIC_SUPER_ADMIN_EMAILS for security
SUPER_ADMIN_EMAILS=admin@example.com

# Cron secret - Required for authenticating scheduled job requests
# Generate a secure random string (e.g., openssl rand -base64 32)
CRON_SECRET=your-secure-cron-secret-here

# ------------------------------------------------------------------------------
# Optional: Analytics & Monitoring
# ------------------------------------------------------------------------------
# Uncomment and add if you're using these services

# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
# SENTRY_DSN=https://...
# NEXT_PUBLIC_POSTHOG_KEY=phc_...

# ==============================================================================
# DEPLOYMENT NOTES
# ==============================================================================
# 
# VERCEL:
# 1. Go to Project Settings > Environment Variables
# 2. Add each variable above
# 3. Set environment: Production, Preview, or Development
# 4. For FIREBASE_PRIVATE_KEY: Paste the ENTIRE key with \n as literal text
#
# SECURITY CHECKLIST:
# ✓ .env.local is in .gitignore
# ✓ Never commit API keys to Git
# ✓ Use different keys for development and production
# ✓ Rotate keys if accidentally exposed
# ✓ Enable Stripe webhook endpoint verification
# ✓ Set up Firebase security rules (see firestore.rules)
#
# ==============================================================================
